#summary Known Issues / Assumptions

  # Assumes that user has read/write permission files and folders being synchronized.
  # Empty folder are not synchronized.
  # No file size capacity check of intermediary storage.
  # The DropBox sync is not yet stable.